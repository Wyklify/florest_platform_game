name: Trigger Vercel Deploy

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Call Vercel Deploy Hook
        run: |
          curl --fail-with-body -X POST "https://api.vercel.com/v1/integrations/deploy/prj_HBciyRIQOsX4zRJXtRDQswrTGdyd/kryZj66tWh"
